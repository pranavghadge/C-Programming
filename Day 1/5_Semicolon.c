// Program that emphasizes the fact that statements in C are terminated with a semicolon
#include<stdio.h>

main(){
    // int valid_name = 20
    int valid_name = 20;
    printf("%d",valid_name);
}